# BarLeads Client Portal

Cloudflare Pages app: static portal (`public/`) + Pages Functions (`functions/api/`), D1 for venue state, R2 for media, Cloudflare Access (email OTP) for login.

Deploys automatically from `main` via Cloudflare Pages git integration.
